import { ExchangeRequestProductList } from '@features/exchange-request-product-list';
import { SafeAreaView } from 'react-native-safe-area-context';


const ProductListPage = () => {
  return <SafeAreaView style={{ flex: 1 }}><ExchangeRequestProductList /></SafeAreaView>
}

export default ProductListPage